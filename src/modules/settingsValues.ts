export interface SettingsValues {
  historyMode: boolean;
  scoreBoardMode: boolean;
  boardSize: number;
  winningAnimationMode: boolean;
}

// theme,
// timer,
// sound,
// boardSize,
// winningAnimation
